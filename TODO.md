# Sandlot wishlist

- Docblocks for macro-generated objects
  - will probably require rewriting the `resource_*!` family of macros
- Custom blend modes
  - switch from an enum to a newtype, along with some constants
